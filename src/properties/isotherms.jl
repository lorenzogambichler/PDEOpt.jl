# Adsorption isotherms — equilibrium loading q*(C, T) (skeleton).
# Langmuir: q* = qsat·b(T)·C / (1 + b(T)·C)
# Consumed by the PSA adsorption (LDF) coupling.
langmuir(args...) = error("TODO: Langmuir isotherm")
