# Ergun equation — packed-bed pressure drop / superficial velocity (skeleton).
# Δp/L = 150·μ(1-ε)²/(ε³ dp²)·u  +  1.75·ρ(1-ε)/(ε³ dp)·u|u|
# Consumed by state-dependent transport (methanation): velocity from Δp.
ergun_velocity(args...) = error("TODO: Ergun velocity / pressure-drop correlation")
