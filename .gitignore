tests/results/
apps/forward_solvers/results/
apps/optimal_control/results/
*.vtu
*.pvd
*.vtr

# Licensed HSL binaries — not redistributable. They stay outside the repo and
# are pulled in via the `hsl` build context (see docker/Dockerfile); these
# entries are a safety net in case a copy ever lands here.
vendor/
HSL_jll*/
